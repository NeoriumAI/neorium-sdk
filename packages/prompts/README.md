# @neorium/prompts

NEORIUM (neo) — Your DeFi Co-Pilot.

Read-only package:
- Default system prompt (`DEFAULT_NEO_SYSTEM_PROMPT`)
- DeFi tool schemas (`NEO_DEFI_TOOL_SCHEMAS`)

This package contains no HTTP logic and no runtime side effects.
